apiVersion: 1
datasources:
  - name: grafana-postgresql-datasource
    type: postgres
    url: IP:PORT
    user: SET THIS
    basicAuth: false
    isDefault: true
    secureJsonData:
      password: SET THIS
    jsonData:
      database: forest
      sslmode: disable
    editable: true